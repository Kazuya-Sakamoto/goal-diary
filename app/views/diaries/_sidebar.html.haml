- Category.all.each do |category|
  .main__sidebar__link
    .main__sidebar__link__box
      = link_to category_path(category.id), class: 'side-bar-link' do
        %li.main__sidebar__link__box__menu #{category.name}
